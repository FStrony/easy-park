$(function() {
	// Configuração para campos de Real.
	$(".valorMonetario").maskMoney({
		showSymbol : true,
		symbol : "R$",
		decimal : ",",
		thousands : "."
	});

});

function ajustaCamposMonetarios() {
	// Configuração para campos de Real.
	$(".valorMonetario").maskMoney({
		showSymbol : true,
		symbol : "R$",
		decimal : ",",
		thousands : "."
	});
}